# Application dependencies

require:
    - play
    - play -> bhave
    - local -> bhave:
        id: test
    
repositories:
    - My modules:
        type:       local
        artifact:   ${application.path}/../bhave/[module]
        contains:
            - local

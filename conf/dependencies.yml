# Application dependencies

require:
    - play
    - behave -> behave
    
repositories:
    - My modules:
        type:       local
        artifact:   ${application.path}/../[module]
        contains:
            - behave

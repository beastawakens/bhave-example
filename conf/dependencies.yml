# Application dependencies

require:
    - play
    - bhave -> bhave
    
repositories:
    - My modules:
        type:       local
        artifact:   ${application.path}/../bhave/[module]
        contains:
            - bhave
